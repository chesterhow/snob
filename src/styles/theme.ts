export interface Theme {
  colors: {
    primary: string;
  };
  font: {
    serif: string;
  };
}
const theme: Theme = {
  colors: {
    primary: '#fff',
  },
  font: {
    serif: 'Fraunces',
  },
};

export default theme;
